<%= turbo_frame_tag "form_modal" do %>
  <turbo-stream target="folder_form" action="replace">
    <template>
      <%= render partial: "form", locals: { space_folder: @space_folder, space: @space, title: "Add New Folder", subtitle: "Folders group which can be added inside a space", url: space_folders_path, method: "post" } %>
    </template>
  </turbo-stream>
<% end %>
