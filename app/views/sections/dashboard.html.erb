<!--
  This example requires Tailwind CSS v2.0+ 
  
  This example requires some changes to your config:
  
  ```
  // tailwind.config.js
  const colors = require('tailwindcss/colors')
  
  module.exports = {
    // ...
    theme: {
      extend: {
        colors: {
          rose: colors.rose,
        },
      },
    },
    plugins: [
      // ...
      require('@tailwindcss/forms'),
    ],
  }
  ```
-->
<!--
  This example requires updating your template:

  ```
  <html class="h-full bg-gray-100">
  <body class="h-full">
  ```
-->
<div class="min-h-full">
  <div class="py-10">
    <div class="max-w-full pl-2 xl:grid xl:grid-cols-12  min-h-full ">
      <div class=" lg:block lg:col-span-3 xl:col-span-2 h-[calc(100vh-6.0rem)] top-16 max-content  overflow-y-scroll px-2 py-2 border-2 overscroll-contain border-r min-h-full">
        <div class="h-full relative" style="min-height: 12rem">
          <div class="absolute inset-0 border-2 border-gray-200 border-dashed rounded-lg"></div>
        </div>
      </div>
      <main class="lg:col-span-3 xl:col-span-7  overflow-y-auto border-r h-[calc(100vh-6.0rem)] top-16 max-content px-2 py-2 border-2  overscroll-contain border-r min-h-full">
        <div class="h-full relative" style="min-height: 12rem">
          <div class="absolute inset-0 border-2 border-gray-200 border-dashed rounded-lg"></div>
        </div>
      </main>
      <aside class="hidden xl:block xl:col-span-3  overflow-y-auto border-r h-[calc(100vh-6.0rem)] top-16 max-content px-2 py-2 border-2 overscroll-contain border-r min-h-full">
        <!-- Start right column area -->
        <div class="h-full relative" style="min-height: 16rem">
          <div class="absolute inset-0 border-2 border-gray-200 border-dashed rounded-lg"></div>
        </div>
        <!-- End right column area -->
      </aside>
    </div>
  </div>
</div>
